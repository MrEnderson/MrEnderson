[project]
name = "jarvis-os"
version = "0.1.1"
description = "Jarvis OS v0.1.1 - personal AI CEO / business operating system orchestration foundation"
requires-python = ">=3.11"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
pythonpath = ["."]

[tool.setuptools]
packages = ["app"]
